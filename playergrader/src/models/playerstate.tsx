// PlayerState.tsx
// import { atom } from 'jotai';

export interface Player {
  id: number;
  name: string;
}

// export const playersAtom = atom<Player[]>([]);

// export const playerAtom = atom<Player>(atom);